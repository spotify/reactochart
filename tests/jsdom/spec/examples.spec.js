import _ from "lodash";
import React from "react";
// import TestUtils from 'react-addons-test-utils';
import { expect } from "chai";

// TODO: fix this to work with new examples structure??

// test the examples render

// import {examples} from '../../../docs/src/Examples.jsx';

// describe('examples', () => {
//   it('has examples', () => {
//     expect(examples.length).to.be.at.least(1);
//   });
//
//   examples.forEach(example => {
//     it(`renders the ${example.title} example`, () => {
//       let ok = true;
//       let svg;
//       try {
//         const ExampleComponent = example.Component;
//         const wrapped = TestUtils.renderIntoDocument(<ExampleComponent />);
//         svg = TestUtils.scryRenderedDOMComponentsWithTag(wrapped, 'svg');
//       } catch(e) { ok = false; }
//
//       expect(ok).to.equal(true);
//       expect(svg.length).to.be.at.least(1);
//     });
//   })
// });
